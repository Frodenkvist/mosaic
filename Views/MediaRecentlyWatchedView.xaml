<UserControl x:Class="Mosaic.Views.MediaRecentlyWatchedView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Mosaic.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:MediaRecentlyWatchedViewModel}"
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid>
        <!-- Empty state -->
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center"
                    Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVisibility}}">
            <TextBlock Text="🍿" FontSize="46" HorizontalAlignment="Center" Opacity="0.5" />
            <TextBlock Text="Nothing watched yet" Foreground="{StaticResource TextSecondary}" FontSize="20"
                       HorizontalAlignment="Center" Margin="0,8,0,0" />
            <TextBlock Text="Movies and shows you play will appear here, most recently watched first."
                       Foreground="{StaticResource TextMuted}" FontSize="14" Margin="0,6,0,0"
                       HorizontalAlignment="Center" />
        </StackPanel>

        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="0,0,6,0">
            <ItemsControl ItemsSource="{Binding Items}" ItemTemplate="{StaticResource MediaTile}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
